Installation process to deploy istio

Step 1. Install istio client on local machine

./istioclient.sh

Step2. Install the istio control plane and Ingress Gate
This also installs internal loadbalancer

./install-istio.sh